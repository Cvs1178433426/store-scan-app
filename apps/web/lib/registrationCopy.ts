export function registrationDeliveryHelp(maskedDestination: string): string {
  return `If your registration can continue, a 6-digit code will arrive at ${maskedDestination}. Delivery may take up to a minute.`;
}
